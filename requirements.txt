# Voice bot patient simulator — pinned to a verified working set (Python 3.11+).
# Pipecat pulls in the service SDKs via extras.
pipecat-ai[anthropic,deepgram,cartesia,silero,twilio]==1.4.0
anthropic==0.111.0
fastapi==0.138.0
uvicorn[standard]==0.49.0
twilio==9.10.9
pyngrok==8.1.2
python-dotenv==1.2.2
loguru==0.7.3
requests==2.34.2
soundfile==0.14.0
numpy==2.4.6
